name: Build Docker
run-name: ${{ github.actor }} builds docker image
on: [push, pull_request]
jobs:
  Build-Docker:
    runs-on: self-hosted
    steps:
      - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
      - run: echo "🔎 The name of your branch is ${{ github.ref }} and your repository is ${{ github.repository }}."
      - name: Check out repository code
        uses: actions/checkout@v3
      - run: echo "💡 The ${{ github.repository }} repository has been cloned to the runner."
      - run: echo "🖥️ The workflow is now ready to test your code on the runner."
      - name: List files in the repository
        run: |
          ls ${{ github.workspace }}
      - run: docker build . --tag rgl:latest
      - run: docker run --net=host --gpus all -v $(pwd):/code -v ${OptiX_INSTALL_DIR}:/optix -e OptiX_INSTALL_DIR=/optix -e NVIDIA_DRIVER_CAPABILITIES=all -it rgl:latest /bin/bash
      - run: ./setup.py --clean-build --with-pcl
      - run: ./build/test/RobotecGPULidar_test
      - run: echo "🍏 This job's status is ${{ job.status }}."
